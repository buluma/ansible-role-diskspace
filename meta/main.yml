---
galaxy_info:
  author: buluma
  role_name: diskspace
  description: "Check diskspace (or inodes) available, fail if too low."
  license: Apache-2.0
  company: ShadowNet
  min_ansible_version: "2.12"
  galaxy_tags:
    - diskspace
    - inodes
    - assert
    - check
dependencies: []
